spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/pgk_food_test
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: P@ssw0rd

  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
        show_sql: false

  flyway:
    enabled: true
    baseline-on-migrate: true
    locations: classpath:db/migration

  sql:
    init:
      mode: never
server:
  forward-headers-strategy: framework

jwt:
  secret: TestSecretKeyForPostgresIT_ChangeMe
  lifetime: 86400000

logging:
  level:
    root: INFO
    com.example.demo: DEBUG

app:
  business-zone: Europe/Samara

security:
  qr-offline-public: false
  rate-limit:
    enabled: false
    use-forwarded-ip: false
